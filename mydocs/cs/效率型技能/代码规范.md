```cpp
/*空格*/
    // , ; 后加空格
    printf("%d\n", a
    for (i = 0; i < n; i++)
    
    //双目运算符 + - * / % || && != == >= <= > < >> << = += -= *= /= %= <<= >>= &= ^= |= 前后均有空格
    a = a + 1
    a < 1 || b > 0
    
    //三目运算符需要空格
    a ? b : c
    
    // -> . 无需空格
    p->a
    A.b
    
    //{} 前和部分 () 前加空格
    if () {
    }
    switch () {
    }
    for () {
    }
    while () {
    }
    do {
    } while ()
    
    
/*选择语句*/
    //三者都未使用 {} 包裹
    if ( )
        //一条语句或一个代码块
    else if ( ) 
        //一条语句或一个代码块
    else 
        //一条语句或一个代码块
    
    //若三者有一个用了 {} 包裹，则三者都需要 {} 包裹
    if ( ) {
        
    } else if ( ) {
    
    } else {
    
    }
    
/*循环语句*/ 一律加上 {} 
    while ( ) {
    
    }
    
    do {
    
    } while ( )
    
    for ( ; ; ) {
    
    }
```